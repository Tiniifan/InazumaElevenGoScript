{
  "name": "CMND_SND_IS_LOADING",
  "kind": "native",
  "category": "sound",
  "description": {
    "en": "Tells whether a stream is still loading. The events always wait on it with suspend() between opening a sound and playing it.\nSound-effect handles (CMND_SND_OPEN_SYS_SE) are never loading.\nSound handles are integers. Streams opened with the CMND_SND_OPEN_* commands (BGM, event sounds, voices, Special Move sounds, map ambience) get a handle that refers to a stream channel. CMND_SND_OPEN_SYS_SE returns a packed number that names a sound effect instead."
  },
  "params": [
    {
      "name": "hSnd",
      "type": "integer",
      "required": true,
      "description": {
        "en": "Sound handle from a CMND_SND_OPEN_* or CMND_SND_GET_*_SNDID call."
      }
    }
  ],
  "returns": {
    "type": "boolean|null",
    "description": {
      "en": "true while the stream is still loading, false once it is ready.\nnull if the script system is not ready or no argument is given."
    }
  },
  "examples": [
    {
      "case": "Open, wait, play (real usage, from ev42023000.nut)",
      "source": "real",
      "code": "hEvSe = CMND_SND_OPEN_EVENT(\"evt_050\");\n\nwhile (CMND_SND_IS_LOADING(hEvSe))\n{\n\tsuspend();\n}\n\nCMND_SND_PLAY(hEvSe);"
    }
  ]
}
