{
  "name": "AddPartyNum_Message_NoFadeBGM_Back",
  "kind": "ev_main",
  "category": "party",
  "description": {
    "en": "Same as AddPartyNum_Message_Back, but without fading/stopping the current BGM (passes -1 for both the fade and wait durations)."
  },
  "params": [
    { "name": "_add_num", "type": "integer", "required": true, "description": { "en": "Number of members to add back." } },
    { "name": "_aParamID", "type": "array", "required": true, "description": { "en": "Array of param IDs, one per member." } },
    { "name": "_aPtLock", "type": "array", "required": true, "description": { "en": "Array of position-lock flags, one per member." } },
    { "name": "_aAvatarEnable", "type": "array", "required": true, "description": { "en": "Array of Fighting Spirit-enable flags, one per member." } },
    { "name": "_aAvatarLock", "type": "array", "required": true, "description": { "en": "Array of Fighting Spirit-lock flags, one per member." } },
    { "name": "_aLv", "type": "array", "required": true, "description": { "en": "Array of levels to set, one per member (-1 to leave unchanged)." } }
  ],
  "returns": {
    "type": "null",
    "description": { "en": "No explicit return statement — Squirrel functions implicitly return null." }
  },
  "examples": [
    {
      "case": "Bringing three party members back in sequence without fading the BGM (not found in script_clean/; invented, following AddPartyNum_Message's real usage pattern)",
      "source": "invented",
      "code": "// Bring three party members back in sequence with the \"rejoined\" presentation, without fading the BGM\nlocal charaID = [\"para_cp0001\", \"para_cp0013\", \"para_cp0002\"];\nlocal ptLock = [true, true, true];\nlocal avaEnable = [false, false, false];\nlocal avaLock = [false, false, false];\nlocal level = [-1, -1, -1];\nAddPartyNum_Message_NoFadeBGM_Back(3, charaID, ptLock, avaEnable, avaLock, level);"
    }
  ]
}
