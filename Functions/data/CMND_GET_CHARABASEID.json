{
  "name": "CMND_GET_CHARABASEID",
  "kind": "native",
  "category": "party",
  "description": {
    "en": "Resolves a roster character ID (name-hash string or numeric ID) to its base character ID."
  },
  "params": [
    { "name": "charaId", "type": "string|integer", "description": { "en": "Character ID (a name-hash string or numeric ID) to resolve." } }
  ],
  "returns": {
    "type": "integer|null",
    "description": {
      "en": "The character's base ID (0 if it doesn't resolve to a known character); null only if the call itself was invalid (no argument given)."
    }
  },
  "example": {
    "source": "real",
    "code": "// Resolve a character ID to its base ID, then look up their figure\nlocal baseID = CMND_GET_CHARABASEID(charaID);\nlocal fig = CMND_GET_CHARA_FIGURE(baseID);"
  }
}
