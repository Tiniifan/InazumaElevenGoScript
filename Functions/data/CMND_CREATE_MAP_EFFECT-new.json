{
  "name": "CMND_CREATE_MAP_EFFECT",
  "kind": "native",
  "category": "map",
  "description": {
    "en": "Creates a map effect object by name and returns its handle. Delete it with CMND_DELETE_MAP_EFFECT. No shipped script calls it."
  },
  "params": [
    {
      "name": "name",
      "type": "string",
      "required": true,
      "description": {
        "en": "Name of the map effect."
      }
    }
  ],
  "returns": {
    "type": "handle|null",
    "description": {
      "en": "Handle of the effect object.\nnull if the script system is not ready or no argument is given."
    }
  },
  "examples": [
    {
      "case": "Creating and removing a map effect (not found in script_clean/; invented)",
      "source": "invented",
      "code": "local hEff = CMND_CREATE_MAP_EFFECT(\"effect_name\");\nWaitLoading(hEff);\n// ...\nCMND_DELETE_MAP_EFFECT(hEff);"
    }
  ]
}
