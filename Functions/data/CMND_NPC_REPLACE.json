{
  "name": "CMND_NPC_REPLACE",
  "kind": "native",
  "category": "scene",
  "description": {
    "en": "Re-evaluates and refreshes the map's NPCs against current story/flag state (e.g. swapping which NPCs appear after story progress). Runs asynchronously; use CMND_IS_NPC_REPLACING to wait for it to finish."
  },
  "params": [],
  "returns": {
    "type": "boolean|null",
    "description": {
      "en": "True once the call has taken effect; null if the call was invalid (e.g. too few arguments, or required engine/script state not ready)."
    }
  },
  "example": {
    "source": "real",
    "code": "// Refresh map NPCs after a story flag changed\nCMND_NPC_REPLACE();\nwhile (CMND_IS_NPC_REPLACING())\n{\n\tsuspend();\n}"
  }
}
