{
  "name": "CMND_DEL_ITEM",
  "kind": "native",
  "category": "party",
  "description": {
    "en": "Removes a given quantity of an item from the player's inventory, by item ID (the counterpart to CMND_ADD_ITEM)."
  },
  "params": [
    { "name": "itemId", "type": "string", "description": { "en": "ID of the item to remove, e.g. \"iii0002\"." } },
    { "name": "quantity", "type": "integer", "description": { "en": "Number of the item to remove." } }
  ],
  "returns": {
    "type": "boolean|null",
    "description": {
      "en": "True once the call has taken effect; null if the call was invalid (fewer than 2 arguments, or required engine/script state not ready)."
    }
  },
  "example": {
    "source": "real",
    "code": "// Unlock talk again, then consume a key item used by this event\nCMND_MESWIN_UNLOCK();\nCMND_DEL_ITEM(\"iii0002\", 1);"
  }
}
