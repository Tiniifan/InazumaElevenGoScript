{
  "name": "CMND_BTL_SET_FORCE_WIN",
  "kind": "native",
  "category": "battle",
  "description": {
    "en": "Forces the outcome of clashes involving a given team/player to always succeed (or, when disabled, stops forcing it), letting scripted matches guarantee a specific player wins their duels."
  },
  "params": [
    { "name": "enable", "type": "boolean", "description": { "en": "True to force wins, false to stop forcing them." } },
    { "name": "teamOrCharaId", "type": "integer|string", "description": { "en": "Team ID (when called with 3-4 arguments) or a character ID string (when called with exactly 2 arguments)." } },
    { "name": "playerNum", "type": "integer", "description": { "en": "Optional player number within the team; only used in the 3-4 argument forms." } },
    { "name": "mode", "type": "integer", "description": { "en": "Optional extra mode parameter; only used in the 4-argument form." } }
  ],
  "returns": {
    "type": "null",
    "description": {
      "en": "Always null — the native implementation never pushes a value onto the stack on success; null is also returned if fewer than 2 arguments were given."
    }
  },
  "example": {
    "source": "real",
    "code": "// Force team 0's players 0 and 1 to always win their clashes for this scripted match\nCMND_BTL_SET_FORCE_WIN(true, 0, 1, true);\nCMND_BTL_SET_FORCE_WIN(true, 0, 0, true);"
  }
}
