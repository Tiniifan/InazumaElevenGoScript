{
  "name": "CMND_BTLEV_MOVEBALL",
  "kind": "native",
  "category": "battle",
  "description": {
    "en": "Moves the ball model to a given position during a scripted match event — the ball counterpart to CMND_BTLEV_MOVECHARA."
  },
  "params": [
    { "name": "position", "type": "array", "description": { "en": "3-element array [x, y, z] with the target position." } },
    { "name": "mode", "type": "integer", "description": { "en": "Movement mode/duration parameter." } }
  ],
  "returns": {
    "type": "boolean|null",
    "description": {
      "en": "True once the move has been set up; null if fewer than 2 arguments were given."
    }
  },
  "example": {
    "source": "invented",
    "code": "// Move the ball to a specific position during a scripted event\nlocal targetPos = [0.0, 0.0, 0.0];\nCMND_BTLEV_MOVEBALL(targetPos, 0);"
  }
}
