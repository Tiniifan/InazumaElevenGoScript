{
  "name": "AddPartyNum_Message",
  "kind": "ev_main",
  "category": "party",
  "description": {
    "en": "Multi-member version of AddParty_Message: adds several party members in sequence, each with the standard \"J_08\" jingle and \"sysmes-add_party\" message presentation and a BGM fade — a fixed-preset wrapper around AddParty_MessageEx."
  },
  "params": [
    { "name": "_add_num", "type": "integer", "required": true, "description": { "en": "Number of members to add." } },
    { "name": "_aParamID", "type": "array", "required": true, "description": { "en": "Array of param IDs, one per member to add." } },
    { "name": "_aPtLock", "type": "array", "required": true, "description": { "en": "Array of position-lock flags, one per member." } },
    { "name": "_aAvatarEnable", "type": "array", "required": true, "description": { "en": "Array of Fighting Spirit-enable flags, one per member." } },
    { "name": "_aAvatarLock", "type": "array", "required": true, "description": { "en": "Array of Fighting Spirit-lock flags, one per member." } },
    { "name": "_aLv", "type": "array", "required": true, "description": { "en": "Array of levels to set, one per member (-1 to leave unchanged)." } }
  ],
  "returns": {
    "type": "null",
    "description": { "en": "No explicit return statement — Squirrel functions implicitly return null." }
  },
  "examples": [
    {
      "case": "Adding three party members in sequence (real usage, from ev42013800.nut)",
      "source": "real",
      "code": "// Add three party members in sequence with the standard recruitment presentation\nlocal charaID = [\"para_cp0001\", \"para_cp0013\", \"para_cp0002\"];\nlocal ptLock = [true, true, true];\nlocal avaEnable = [false, false, false];\nlocal avaLock = [false, false, false];\nlocal level = [-1, -1, -1];\nAddPartyNum_Message(3, charaID, ptLock, avaEnable, avaLock, level);"
    }
  ]
}
