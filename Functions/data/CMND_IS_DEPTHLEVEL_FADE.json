{
  "name": "CMND_IS_DEPTHLEVEL_FADE",
  "kind": "native",
  "category": "camera",
  "description": {
    "en": "Checks whether a camera's DepthLevel animation started with CMND_DEPTHLEVEL_FADE is still in progress. Never called from any shipped script in script_clean/; behavior reconstructed from the native implementation and its similarity to the other IS_*_FADE commands."
  },
  "params": [
    {
      "name": "cameraHandle",
      "type": "handle",
      "description": {
        "en": "Camera handle to check."
      }
    }
  ],
  "returns": {
    "type": "boolean",
    "description": {
      "en": "True while the DepthLevel animation is still in progress."
    }
  },
  "example": {
    "source": "invented",
    "code": "// Wait for the camera's depth level animation to finish\nwhile (CMND_IS_DEPTHLEVEL_FADE(hCam))\n{\n\tsuspend();\n}"
  }
}
