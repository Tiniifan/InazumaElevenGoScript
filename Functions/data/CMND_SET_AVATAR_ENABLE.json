{
  "name": "CMND_SET_AVATAR_ENABLE",
  "kind": "native",
  "category": "party",
  "description": {
    "en": "Enables or disables a character's currently attached avatar (keshin/mascot)."
  },
  "params": [
    {
      "name": "handle",
      "type": "handle",
      "description": {
        "en": "Character handle to configure."
      }
    },
    {
      "name": "enabled",
      "type": "boolean",
      "description": {
        "en": "True to enable the avatar."
      }
    }
  ],
  "returns": {
    "type": "boolean|null",
    "description": {
      "en": "True once the call has taken effect; null if the call was invalid (e.g. too few arguments, or required engine/script state not ready)."
    }
  },
  "example": {
    "source": "real",
    "code": "// Get a roster character's handle, then temporarily force their avatar on before attaching a new one\nlocal hdl = CMND_GET_PARTY_HANDLE(_chr);\nlocal wasLocked = CMND_GET_AVATAR_LOCK_FLAG(hdl);\nCMND_SET_AVATAR_ENABLE(hdl, true);\nCMND_SET_AVATAR_LOCK_FLAG(hdl, false);\nCMND_ATTACH_AVATAR(_chr, _avt);\nCMND_SET_AVATAR_LOCK_FLAG(hdl, wasLocked);"
  }
}
