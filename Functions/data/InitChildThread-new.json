{
  "name": "InitChildThread",
  "kind": "ev_main",
  "category": "system",
  "description": {
    "en": "Prepares room for child threads in the current event: it creates subev_ctx.c_coros with _num empty slots and resets the thread count. Call it before AddChildThread, then run the threads with RunChildThread."
  },
  "params": [
    {
      "name": "_num",
      "type": "integer",
      "required": true,
      "description": {
        "en": "Number of child threads that will be added."
      }
    }
  ],
  "returns": {
    "type": "null",
    "description": {
      "en": "Always null (no return statement)."
    }
  },
  "examples": [
    {
      "case": "Three threads (real usage, from ev42010300.nut)",
      "source": "real",
      "code": "InitChildThread(3);\nAddChildThread(fadeinProc);\nAddChildThread(TextProc);\nAddChildThread(TenmaProc);\nRunChildThread();"
    }
  ]
}
