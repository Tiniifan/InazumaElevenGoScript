{
  "name": "CMND_SND_OPEN_VOICE",
  "kind": "native",
  "category": "sound",
  "description": {
    "en": "Opens a voice clip by name and returns its handle, to start with CMND_SND_PLAY after CMND_SND_IS_LOADING. No shipped script calls it.\nSound handles are integers. Streams opened with the CMND_SND_OPEN_* commands (BGM, event sounds, voices, Special Move sounds, map ambience) get a handle that refers to a stream channel. CMND_SND_OPEN_SYS_SE returns a packed number that names a sound effect instead."
  },
  "params": [
    {
      "name": "name",
      "type": "string",
      "required": true,
      "description": {
        "en": "Voice clip name."
      }
    }
  ],
  "returns": {
    "type": "integer|null",
    "description": {
      "en": "Handle of the clip; 0 if the name is missing.\nnull if the script system is not ready or no argument is given."
    }
  },
  "examples": [
    {
      "case": "Playing a voice clip (not found in script_clean/; invented)",
      "source": "invented",
      "code": "local hVoice = CMND_SND_OPEN_VOICE(voiceName);\n\nwhile (CMND_SND_IS_LOADING(hVoice))\n{\n\tsuspend();\n}\n\nCMND_SND_PLAY(hVoice);"
    }
  ]
}
