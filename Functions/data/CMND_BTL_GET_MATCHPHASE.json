{
  "name": "CMND_BTL_GET_MATCHPHASE",
  "kind": "native",
  "category": "battle",
  "description": {
    "en": "Reads back the current match phase set with CMND_BTL_SET_MATCHPHASE."
  },
  "params": [],
  "returns": {
    "type": "integer|null",
    "description": {
      "en": "The current match phase value; null if the call was invalid."
    }
  },
  "example": {
    "source": "real",
    "code": "// Trigger a scripted event once the score and match phase reach specific values\nif (CMND_BTL_GET_POINT(0) == 1 && CMND_BTL_GET_MATCHPHASE() == 100 && eventCounter == 0)\n{\n\t// ... trigger event ...\n}"
  }
}
