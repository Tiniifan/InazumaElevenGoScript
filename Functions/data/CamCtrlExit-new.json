{
  "name": "CamCtrlExit",
  "kind": "ev_main",
  "category": "camera",
  "description": {
    "en": "Ends scripted camera control: replaces the camera-control state with an empty one. With no camera handle in it, sysCamCtrlStep stops touching the camera. The camera itself is not moved back.\nThe camera-control helpers keep their state in subev_ctx.cam_ctrl, the context of an event started by CallEvent. While such an event runs, CallEvent calls sysCamCtrlStep every frame: it applies the engine's camera move (CMND_GET_MOVE_INFO_CAMERA) to the camera, keeps the look-at point on the followed character when there is one, and marks the move as finished once it is over (see IsCamCtrlMove)."
  },
  "params": [],
  "returns": {
    "type": "null",
    "description": {
      "en": "Always null (no return statement)."
    }
  },
  "examples": [
    {
      "case": "After the last camera move of an event (real usage, from ev42010200.nut)",
      "source": "real",
      "code": "CamCtrlMoveTo(hCam, 80, [-86.699997, 81.699997, -55.900002, -86.699997, 3.5, -159.7]);\nWaitEvLowMdlMoving(hTenmaL);\nWaitCamCtrlMoving(hCam);\nCMND_CHR_WARP_SET(hTenmaL, true);\nCamCtrlExit();"
    }
  ]
}
